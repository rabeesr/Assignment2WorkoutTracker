'use client';

export default function BasketballFields() {
  return (
    <p className="text-sm text-muted italic">
      No additional metrics needed — duration and intensity are sufficient for basketball.
    </p>
  );
}
